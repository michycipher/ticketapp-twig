<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>{% block title %}Tix — TicketApp{% endblock %}</title>

  {# Tailwind CDN for quick prototyping. Replace with built CSS in production. #}
  <script src="https://cdn.tailwindcss.com"></script>

  <style>
    /* Additional focus styles */
    :focus { outline: 2px solid #2563eb; outline-offset: 2px; }
    /* Max width container */
    .container-max { max-width: 1440px; margin-left: auto; margin-right: auto; padding-left: 1rem; padding-right: 1rem; }
  </style>

  {% block head %}{% endblock %}
</head>
<body class="bg-gray-50 text-gray-800 antialiased">
  <div class="container-max">
    <header class="py-6 flex items-center justify-between">
      <a href="/" class="flex items-center gap-3">
        <div class="w-10 h-10 rounded-full bg-indigo-600 text-white flex items-center justify-center font-bold">T</div>
        <div>
          <h1 class="text-xl font-semibold">Tix</h1>
          <p class="text-sm text-gray-500">Ticket management, simplified</p>
        </div>
      </a>
      <nav class="hidden md:flex gap-4 items-center">
        <a href="/tickets" class="text-sm">Tickets</a>
        <a href="/dashboard" class="text-sm">Dashboard</a>
        <a href="/auth/login" class="btn-login text-sm">Login</a>
      </nav>
      <button id="mobile-menu-btn" class="md:hidden">☰</button>
    </header>

    <main>
      {% block content %}{% endblock %}
    </main>

    <footer class="mt-12 py-6 border-t border-gray-200 text-center text-sm text-gray-500">
      © {{ "now"|date("Y") }} Tix — Built with Twig & Tailwind
    </footer>
  </div>

  {# Shared JS files (put in public/js/) #}
  <script src="/js/toasts.js"></script>
  <script src="/js/guard.js"></script>
  <script src="/js/auth.js"></script>
  <script src="/js/tickets.js"></script>
  {% block scripts %}{% endblock %}
</body>
</html>
